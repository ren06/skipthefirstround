label.control-label.col-sm-2(for='videoFile') Video file:
.col-sm-3
    label.btn.btn-default.btn-file(style='width:160px')
        |   Select Video file...
        input#videoFile(type="file" style="display: none;" name="file")
.col-sm-5.vertical-center
    div.progress
        div.progress-bar.progress-bar-striped(role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="text-align:center")
            span#progressText
    input(id="videoProviderUniqueId" type="hidden" name="videoProviderUniqueId" value="#{videoData.providerUniqueId}")
    input(id="videoUrl" type="hidden" name="videoUrl" value="#{videoData.url}")
    span#videoLink
        if(videoData.providerUniqueId !== '')
            a(href='#{videoData.url}') See current Video
        else
            | No video

